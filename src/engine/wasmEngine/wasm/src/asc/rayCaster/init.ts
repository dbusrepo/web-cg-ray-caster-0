import { PTR_T, SIZE_T, NULL_PTR } from '../memUtils';
import { Viewport } from './viewport';

let viewport = changetype<Viewport >(NULL_PTR);

function initRayCaster(): void {

}

export { initRayCaster };

